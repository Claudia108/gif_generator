<h2>Category: '<%= @category.name %>'</h2>

<h3><%= link_to "All Categories", admin_categories_path %></h3>


<% @category.gifs.each_with_index do |gif, index|  %>
  <img src="<%= gif.image_path %>">
  <% if (index + 1)%5 == 0 %><br><% end %>
<% end %>
